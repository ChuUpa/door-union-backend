# Системный мусор macOS — не нужен в репозитории
.DS_Store

# Личные настройки VS Code — у каждого разработчика свои
.vscode/

# Секреты: пароли, токены, строки подключения к БД — никогда не коммитить
.env

# Папка с локальными копиями зависимостей (go mod vendor)
vendor/

# Скомпилированный бинарник — собирается из исходников, в git не нужен
door-union-backend

# Медиафайлы портфолио — большие бинарники, не для git
web/static/images/portfolio/